<!DOCTYPE html>
<html>
<head>
    {{> header}}
    <!-- 외부 CSS 파일 -->
    <link rel="stylesheet" href="/css/output.css">
    <link rel="stylesheet" href="/css/global.css">
</head>
<body class= "output-body">
    <div class="container">
        <!-- 제목 -->
        <h2 class="output-h2-title">가장 가까운 대피소</h2>

        <!-- 결과 테이블 -->
        <table class="output-table">
            <thead>
                <tr>
                    <th>대피소 명</th>
                    <th>대피소 주소</th>
                    <th>거리</th>
                    <th>길안내 링크</th>
                    <th>로드뷰 링크</th>
                </tr>
            </thead>
            <tbody>
                {{#each outputFormList}}
                <tr>
                    <td>{{shelterName}}</td>
                    <td>{{shelterAddress}}</td>
                    <td>{{distance}}</td>
                    <td><a href="{{directionUrl}}" target="_blank">길안내</a></td>
                    <td><a href="{{roadViewUrl}}" target="_blank">로드뷰</a></td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <!-- 이미지 섹션 -->
        <div class="output-grid-image">
            {{#each images}}
            <img src="{{this}}" alt="대피소 이미지">
            {{/each}}
        </div>
    </div>

 <!-- Footer Include -->
    {{> footer}}

</body>
</html>
